import React, {useState} from 'react';
import {
    StyleSheet,
    View, 
    Text, 
    TextInput, 
    TouchableOpacity, 
    Button, 
    FlatList,
    VirtualizedList,
    StatusBar,
    Image,
} from 'react-native';

export default function Produto({navigation})
{

    return(
        <Text>Produtos ...</Text>

    );

}